{
  "name": "to-do-list",
  "version": "1.0.0",
  "description": "To-Do List with multi-tab sync",
  "main": "index.html",
  "scripts": {
    "clean": "rimraf build",
    "prebuild": "npm run clean",
    "build": "mkdir build && cp *.{html,css,js} build/",
    "deploy": "gh-pages -d build"
  },
  "homepage": "https://marcelotigrero.github.io/To-Do-List",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "rimraf": "^6.0.1"
  }
}
